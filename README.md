# Soares Soluções — Atualizações

Repositório público usado exclusivamente para distribuir atualizações do aplicativo Soares Soluções.

O aplicativo consulta o arquivo `update.json` e valida cada arquivo de atualização por SHA-256 antes de aplicá-lo.
